<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <!-- <SelectDialog
		id="selectDialog"
		title="Products"
		items="{/ProductCollection}"
		search=".onValueHelpDialogSearch"
		confirm=".onValueHelpDialogClose"
		cancel=".onValueHelpDialogClose"
        >

		<StandardListItem
			iconDensityAware="false"
			iconInset="false"
			title="{Name}"
			description="{ProductId}" />
	</SelectDialog> -->

    <SelectDialog
        id="deptSelectDialog"
        title="Select Department"
        items="{deptModel>/departments}"
        search=".onDeptSearch"
        confirm=".onDeptConfirm"
        cancel=".onDeptConfirm"
    >
        <StandardListItem title="{deptModel>dept}" />
    </SelectDialog>
</core:FragmentDefinition>
